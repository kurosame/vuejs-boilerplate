import { mount } from '@vue/test-utils'
import Parent from '@/pages/Parent.vue'

describe('pages', () => {
  describe('Parent.vue', () => {
    // const wrapper = mount(Parent)

    test('count', () => {})
  })
})
